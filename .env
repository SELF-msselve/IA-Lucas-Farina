# ==============================
# Environment Variables Template
# ==============================
# Copy this file to `.env` and fill in your values before running the app.
# DO NOT commit your real secrets to version control.

# ---- OpenAI ----
OPENAI_API_KEY=your_openai_api_key_here

# (Optional) If you use a specific OpenAI organization/project
# OPENAI_ORG_ID=
# OPENAI_PROJECT=

# ---- Langfuse (Observability) ----
LANGFUSE_PUBLIC_KEY=your_langfuse_public_key_here
LANGFUSE_SECRET_KEY=your_langfuse_secret_key_here
LANGFUSE_HOST=https://cloud.langfuse.com

# ---- App Settings ----
# Set to true to enable verbose logging in your app
APP_DEBUG=false

# Maximum number of PDF pages to process (safety/cost control)
MAX_PDF_PAGES=50

# Model settings (override defaults in code if needed)
OPENAI_MODEL=gpt-4o-mini
OPENAI_VISION_MODEL=gpt-4o-mini

# Request controls
OPENAI_TEMPERATURE=0
OPENAI_MAX_TOKENS=2000

# ---- Optional Proxy ----
# HTTP_PROXY=
# HTTPS_PROXY=

# ---- Streamlit ----
# Set server port if deploying on custom infra
# STREAMLIT_SERVER_PORT=8501

# ---- Security Notes ----
# - Never share this file publicly with real values.
# - Use environment secrets in your deployment platform (e.g., Docker, CI/CD, or cloud provider).
